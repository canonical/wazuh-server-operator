name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
    with:
      self-hosted-runner: true
      self-hosted-runner-label: self-hosted-linux-amd64-noble-medium
      self-hosted-runner-arch: amd64
      self-hosted-runner-image: noble
      vale-style-check: false
      with-uv: true
