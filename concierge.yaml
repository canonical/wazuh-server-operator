# Copyright 2025 Canonical Ltd.
# See LICENSE file for licensing details.
juju:
  channel: 3/stable
  model-defaults:
    test-mode: "true"
    automatically-retry-hooks: "false"

providers:
  microk8s:
    enable: true
    bootstrap: true
    addons:
      - hostpath-storage
      - dns
      - rbac
      - registry
  lxd:
    enable: true
    bootstrap: true
    model-defaults:
      test-mode: "true"
      automatically-retry-hooks: "false"

host:
  snaps:
    rockcraft:
    charmcraft:

